<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Bluetask.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:views="using:Bluetask.Views"
    xmlns:controls="using:Bluetask.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Bluetask">

    

    <Grid x:Name="RootGrid" Background="{StaticResource App.GradientBackground}">
        <Grid.Resources>
            <!-- Gradient brushes for title bar buttons -->
            <LinearGradientBrush x:Key="TitleButtonHoverBrush" StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#3BA0FF" Offset="0"/>
                <GradientStop Color="#6C70FF" Offset="1"/>
            </LinearGradientBrush>
            <LinearGradientBrush x:Key="TitleButtonPressedBrush" StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#2C7BD9" Offset="0"/>
                <GradientStop Color="#4E53D9" Offset="1"/>
            </LinearGradientBrush>

            <!-- Sleek title bar button style -->
            <Style x:Key="TitleBarButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="BorderBrush" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Padding" Value="0"/>
                <Setter Property="Height" Value="28"/>
                <Setter Property="Width" Value="36"/>
                <Setter Property="CornerRadius" Value="6"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Grid>
                                <Border x:Name="BackgroundBorder" Background="{TemplateBinding Background}" CornerRadius="{TemplateBinding CornerRadius}"/>
                                <ContentPresenter x:Name="IconPresenter" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{TemplateBinding Foreground}"/>
                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="CommonStates">
                                        <VisualState x:Name="Normal"/>
                                        <VisualState x:Name="PointerOver">
                                            <VisualState.Setters>
                                                <Setter Target="IconPresenter.Foreground" Value="{StaticResource TitleButtonHoverBrush}"/>
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="Pressed">
                                            <VisualState.Setters>
                                                <Setter Target="IconPresenter.Foreground" Value="{StaticResource TitleButtonPressedBrush}"/>
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="Disabled"/>
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="34"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        

        <!-- Custom Title Bar -->
        <Grid Grid.Row="0" Height="34" Background="Transparent">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Drag region for moving the window -->
            <Border x:Name="TitleBarDragRegion" Background="Transparent" Margin="64,0,0,0"/>

            <!-- Minimize and Close buttons (no Maximize) -->
            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,8,0">
                <Button x:Name="MinimizeButton" Style="{StaticResource TitleBarButtonStyle}" Click="MinimizeButton_Click">
                    <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE921;" FontSize="12"/>
                </Button>
                <Button x:Name="CloseButton" Style="{StaticResource TitleBarButtonStyle}" Click="CloseButton_Click">
                    <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE8BB;" FontSize="12"/>
                </Button>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <!-- Reserve 64px rail so content is never covered when minimized -->
                <ColumnDefinition Width="64"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Content area to the right of the rail -->
            <Frame x:Name="ContentFrame" Grid.Column="1"/>

        </Grid>

        <!-- Sidebar overlays entire window area; minimized rail remains at 64px due to content grid -->
        <controls:SidebarNavigation x:Name="Sidebar"
                                   NavigationRequested="Sidebar_NavigationRequested"
                                   HorizontalAlignment="Left"
                                   VerticalAlignment="Stretch"
                                   Grid.RowSpan="2"
                                   Canvas.ZIndex="10"/>

    </Grid>
</Window>
