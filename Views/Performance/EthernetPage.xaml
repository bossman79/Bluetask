 <Page
    x:Class="Bluetask.Views.Performance.EthernetPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Bluetask.Views.Performance"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:Bluetask.ViewModels"
    xmlns:converters="using:Bluetask.Services.Converters"
    mc:Ignorable="d"
    Background="Transparent">
    <Page.Resources>
        <converters:HistoryToPointsConverter x:Key="HistoryToPointsConverter"/>
    </Page.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="Ethernet"
                       FontSize="32" 
                       FontWeight="SemiBold" 
                       Foreground="White"/>
            <TextBlock Text="{Binding Network.AdapterName, Mode=OneWay}"
                       FontSize="16" 
                       Foreground="White" 
                       Opacity="0.7"
                       Margin="0,8,0,0"/>
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="220"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#1E2736" CornerRadius="8" BorderBrush="#2A2C36" BorderThickness="1" Margin="0,0,12,0">
                <Grid Margin="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Kbps" FontSize="12" Foreground="White" Opacity="0.6"/>
                        <TextBlock Grid.Column="1" Text="60 seconds" FontSize="12" Foreground="White" Opacity="0.6"/>
                    </Grid>

                    <Grid Grid.Row="1">
                        <Canvas Height="280">
                            <Polyline Points="{Binding ChartHistory, Converter={StaticResource HistoryToPointsConverter}, Mode=OneWay}" Stroke="{StaticResource App.NetworkAccent}" StrokeThickness="2" Fill="Transparent"/>
                        </Canvas>
                    </Grid>
                </Grid>
            </Border>

            <Grid Grid.Column="1" Margin="12,0,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Border Background="#1E2736" CornerRadius="8" BorderBrush="#2A2C36" BorderThickness="1" Grid.Row="0" Margin="0,0,0,6">
                    <StackPanel Margin="16">
                        <TextBlock Text="{Binding Network.Sent, Mode=OneWay}" FontSize="32" FontWeight="Bold" Foreground="White"/>
                        <TextBlock Text="Kbps Sent" Opacity="0.6"/>
                    </StackPanel>
                </Border>
                <Border Background="#1E2736" CornerRadius="8" BorderBrush="#2A2C36" BorderThickness="1" Grid.Row="1" Margin="0,6,0,0">
                    <StackPanel Margin="16">
                        <TextBlock Text="{Binding Network.Received, Mode=OneWay}" FontSize="32" FontWeight="Bold" Foreground="White"/>
                        <TextBlock Text="Kbps Received" Opacity="0.6"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>

        <Border Grid.Row="2" Background="#1E2736" CornerRadius="8" BorderBrush="#2A2C36" BorderThickness="1" Padding="24" Margin="0,24,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Connection type" Opacity="0.7"/>
                    <TextBlock Text="{Binding Network.ConnectionType, Mode=OneWay}" FontWeight="SemiBold"/>
                </StackPanel>
                <StackPanel Grid.Column="1">
                    <TextBlock Text="Link speed" Opacity="0.7"/>
                    <TextBlock Text="{Binding Network.LinkSpeed, Mode=OneWay}" FontWeight="SemiBold"/>
                </StackPanel>
                <StackPanel Grid.Column="2">
                    <TextBlock Text="IPv4 address" Opacity="0.7"/>
                    <TextBlock Text="{Binding Network.IpAddress, Mode=OneWay}" FontWeight="SemiBold"/>
                </StackPanel>
                <StackPanel Grid.Column="3">
                    <TextBlock Text="Status" Opacity="0.7"/>
                    <TextBlock Text="{Binding Network.Status, Mode=OneWay}" FontWeight="SemiBold"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Page>
